apiVersion: v1
kind: Service
metadata:
  name: agentframework-chatbot
spec:
  selector:
    app: agentframework-chatbot
  ports:
    - name: http
      port: 80
      targetPort: 8501
  type: ClusterIP
